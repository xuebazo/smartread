# SmartRead v2.0 — 个人 AI 英语阅读工作台 · 完整使用说明书

---

## 目录

1. [项目概述](#1-项目概述)
2. [系统架构](#2-系统架构)
3. [环境与安装](#3-环境与安装)
4. [启动与运行](#4-启动与运行)
5. [功能详解](#5-功能详解)
   - 5.1 侧边栏与全局状态
   - 5.2 每日外刊推荐（含已读标记）
   - 5.3 文章阅读分析（并发 + 难度 + 计时）
   - 5.4 生词本（含闪卡复习）
   - 5.5 阅读记录与统计（含可视化）
   - 5.6 阅读状态自动恢复
   - 5.7 数据库自动备份
6. [模块说明](#6-模块说明)
7. [数据库表结构](#7-数据库表结构)
8. [API 参考](#8-api-参考)
9. [常见问题](#9-常见问题)

---

## 1. 项目概述

**SmartRead v2.0** 是面向四六级备考者的 AI 驱动英语阅读工作台。

| 功能 | v1.0 | v2.0 |
|------|------|------|
| AI 翻译 | ✅ 串行 | ✅ **4 Agent 并发** |
| 词汇提取 | ✅ | ✅ + ⭐**收藏** |
| 长难句分析 | ✅ | ✅ |
| 难度评级 | ❌ | ✅ **CET4/CET6/考研/IELTS** |
| 生词本 | ❌ | ✅ **搜索 / 标记 / 闪卡复习** |
| 阅读记录 | ❌ | ✅ **统计卡片 + 难度分布图** |
| 状态恢复 | ❌ | ✅ **刷新不丢失** |
| 已读标记 | ❌ | ✅ **✅已读 + 未读过滤** |
| 阅读计时 | ❌ | ✅ **实际分钟数** |
| 打卡统计 | ❌ | ✅ **真连续天数** |
| 数据备份 | ❌ | ✅ **每日自动备份** |
| 可视化 | ❌ | ✅ **词汇增长 + 难度分布** |
| AI 调用 | 各 Agent 独立 | ✅ **统一 AIService** |
| 工程结构 | 扁平 | ✅ **services/database/agents 分层** |

**项目位置**：`D:\smartread`

**技术栈**：`Python 3.10+` · `Streamlit 1.57` · `DeepSeek API` · `SQLite` · `newspaper3k` · `BeautifulSoup 4` · `feedparser` · `nltk` · `pandas`

---

## 2. 系统架构

### 2.1 完整目录结构

```
D:\smartread\
│
├── app.py                       # 主程序（~770行，4页面 + 闪卡复习子流程）
├── config.py                    # API Key 管理
├── rss_fetcher.py               # 5 个 RSS 源聚合
├── article_cache.py             # 文章缓存 + 已读链接查询
├── utils.py                     # 正文提取（双引擎）
├── requirements.txt             # 7 个 Python 依赖
├── .gitignore
├── .env                         # DEEPSEEK_API_KEY
├── cache.db                     # SQLite（5 张表，自动创建）
├── 使用说明书.md
│
├── agents/
│   ├── __init__.py
│   ├── translator.py            # 翻译官（调用 AIService）
│   ├── vocabulary.py            # 词汇导师（调用 AIService）
│   ├── grammar.py               # 语法教练（调用 AIService）
│   └── difficulty.py            # 难度评级（规则 + AI 双模式）
│
├── services/
│   ├── __init__.py
│   ├── ai_service.py            # 统一 AI Service
│   └── backup_service.py        # 数据库每日自动备份
│
├── database/
│   ├── __init__.py
│   ├── vocab_repository.py      # 生词本 CRUD + 图表数据
│   └── history_repository.py    # 阅读历史 + 状态恢复 + 图表数据
│
├── backups/                     # 自动备份目录（首次运行时创建）
│   └── cache_20260517.db
│
└── venv/
```

### 2.2 分层架构

```
┌─────────────────────────────────────────────────┐
│                  Streamlit UI                     │
│          app.py（4页面 · 状态管理）                │
├─────────────────────────────────────────────────┤
│              Agents 业务逻辑层                    │
│  translator / vocabulary / grammar / difficulty  │
├─────────────────────────────────────────────────┤
│              Services 服务层                      │
│      AIService / BackupService                   │
├─────────────────────────────────────────────────┤
│            Database 数据仓储层                    │
│  vocab_repository / history_repository           │
│        article_cache / cache.db                  │
└─────────────────────────────────────────────────┘
```

### 2.3 数据流

```
用户操作 → app.py
              │
              ├─ 每日推荐 ─→ rss_fetcher.py ─→ article_cache.py ─→ cache.db
              │                  └─ get_read_links() 标记已读
              │
              ├─ 阅读分析 ─→ ThreadPoolExecutor(max_workers=4)
              │                  ├── translator.py  ─┐
              │                  ├── vocabulary.py  ─┤─→ AIService ─→ DeepSeek API
              │                  ├── grammar.py     ─┤
              │                  └── difficulty.py  ─┘
              │                  └─ 计时 → history_repository
              │
              ├─ 生词本   ─→ vocab_repository ─→ cache.db
              │                  └─ 闪卡复习
              │
              ├─ 阅读记录 ─→ history_repository ─→ cache.db
              │                  └─ 图表数据
              │
              └─ 启动时   ─→ backup_service.py ─→ backups/
```

---

## 3. 环境与安装

### 3.1 前置条件

- Windows 操作系统（已在 D 盘验证）
- Python 3.10 或更高版本
- 网络连接（API 调用 + RSS 拉取）
- DeepSeek API Key（[platform.deepseek.com/api_keys](https://platform.deepseek.com/api_keys)）

### 3.2 安装步骤

**第一步**：打开终端（Win+R → `powershell` → 回车）

**第二步**：进入项目目录
```powershell
cd D:\smartread
```

**第三步（推荐）**：创建虚拟环境
```powershell
python -m venv venv
venv\Scripts\activate
```

**第四步**：安装依赖
```powershell
pip install -r requirements.txt
```

依赖清单（共 7 个）：

| 包名 | 最低版本 | 用途 |
|------|----------|------|
| `streamlit` | 1.28.0 | Web UI 框架 |
| `requests` | 2.28.0 | HTTP 请求 |
| `python-dotenv` | 1.0.0 | `.env` 环境变量 |
| `feedparser` | 6.0.0 | RSS 解析 |
| `beautifulsoup4` | 4.12.0 | HTML 解析 |
| `newspaper3k` | 0.2.8 | 正文提取引擎① |
| `lxml_html_clean` | 0.4.0 | newspaper3k 必要依赖 |

**第五步**：配置 API Key
```
DEEPSEEK_API_KEY=sk-your-key-here
```
写入 `D:\smartread\.env`。

---

## 4. 启动与运行

```powershell
cd D:\smartread
streamlit run app.py
```

浏览器访问 `http://localhost:8501`。

桌面快捷方式：双击 `SmartRead.bat`。

停止：终端按 `Ctrl + C`。

---

## 5. 功能详解

### 5.1 侧边栏与全局状态

```
┌──────────────┐
│ 📖 SmartRead  │
│              │
│ ○ 📰 每日推荐 │
│ ○ 📖 阅读工具 │
│ ○ 🧠 生词本   │
│ ○ 📚 阅读记录 │
│              │
│ ──────────── │
│ 版本: v2.0   │
│ 路径: D:\... │
│ 🟢 API 已连接 │
└──────────────┘
```

| 元素 | 说明 |
|------|------|
| 导航单选 | 四个页面，点击切换（切换时自动保存阅读时长） |
| 版本号 | v2.0 |
| API 状态 | 🟢已连接 / 🔴未配置密钥 / 🔴连接失败 |

### 5.2 📰 每日外刊精选（含已读标记）

#### 操作流程

1. **刷新**：点击 `🔄 刷新文章列表` → 并行拉取 5 源 → 去重 → 存入 cache.db → 清理 30 天前记录

2. **筛选**：
   - 来源：`全部` / `纽约时报` / `卫报` / `科学美国人` / `BBC News` / `NPR`
   - 已读：☑ `仅显示未读` — 勾选后隐藏已分析过的文章

3. **已读标记**：
   - 已分析过的文章显示 `✅ 已读 / 重读` 按钮（卡片标题变灰）
   - 未读文章保持 `📖 开始阅读` 按钮
   - 标记持久存储在 `reading_history` 表，重启不丢失

4. **开始阅读**：自动保存上一篇文章的阅读时长 → 双引擎提取全文 → 跳转

#### 预置 RSS 源

| 来源 | RSS 地址 | 领域 | 风格 |
|------|----------|------|------|
| 纽约时报 | `rss.nytimes.com/.../World.xml` | 综合新闻 | 美式英语 |
| 卫报 | `theguardian.com/world/rss` | 综合新闻 | 英式英语 |
| 科学美国人 | `sciencenews.org/feed` | 科学短讯 | 科普阅读 |
| BBC News | `feeds.bbci.co.uk/news/world/rss.xml` | 综合新闻 | 四六级题源 |
| NPR | `feeds.npr.org/1001/rss.xml` | 综合新闻 | 美式口语 |

### 5.3 📖 文章阅读分析（核心）

#### 并发执行机制

点击 `🚀 开始智能阅读` → `ThreadPoolExecutor(max_workers=4)` 同时启动：

```
┌─ translate_article(text)  ──→ translation
├─ extract_vocabulary(text) ──→ vocabulary
├─ analyze_grammar(text)    ──→ grammar
└─ analyze_difficulty(text) ──→ difficulty

任意失败 → 其他继续 → 全部完成 → 渲染结果
```

#### 阅读计时

分析完成后自动开始计时（`time.time()`）。切换页面或打开新文章时：
- 计算实际分钟数 `elapsed = int((now - start) / 60)`
- 写入 `reading_history.reading_minutes`
- 阅读记录中显示 `⏱️ X 分钟`，0 分钟显示 `⏱️ —`

#### 难度评级

| 等级 | 星标 | 判定条件 |
|------|------|----------|
| CET4 | ★☆☆☆☆ ~ ★★☆☆☆ | CET6 词汇占比 < 2% |
| CET6 | ★★★☆☆ | CET6 词汇占比 > 2% |
| 考研 | ★★★★☆ | CET6 词汇占比 > 4% + 平均句长 > 18 |
| IELTS | ★★★★★ | CET6 词汇占比 > 6% + 平均句长 > 22 |

优先 AI 分类，失败降级为规则判断（500+ 词汇库）。

#### 三个标签页

- **📖 中英对照**：逐段原文（灰底）+ 译文（蓝底），JSON 失败降级原始文本
- **📝 核心词汇**：每词 `☆` / `⭐` 收藏按钮
- **🧠 长难句分析**：1-2 句卡片，Markdown 语法拆解

### 5.4 🧠 生词本（含闪卡复习）

#### 列表模式

- 搜索：按单词或释义模糊匹配
- 展开卡片：例句、来源、收藏时间、复习次数
- `✅ 认识`：`known_level=1`
- `🎓 掌握`：`known_level=2`
- `🗑️ 删除`

#### 闪卡复习模式 🃏

点击 `🃏 开始复习` 进入：

```
第 3 / 10 张  ████████░░░░░░░░

### ubiquitous
/ juːˈbɪkwɪtəs /

[👁️ 显示答案]

--- 点击后 ---

释义：无处不在的
📝 Smartphones have become ubiquitous in modern society.

[✅ 认识了]  [❌ 还不会]  [🛑 结束复习]
```

- 优先新词（`known_level=0`），其次认识中的词
- 每次 10 张卡片，按复习次数升序
- 「认识了」→ `known_level+1`（最多到 2）
- 「还不会」→ 仅 `review_count+1`，下次仍会出现
- 结束后显示复习报告：总共 / 认识 / 还需努力

### 5.5 📚 阅读记录与统计

#### 统计仪表盘

```
┌──────────┬──────────┬──────────┬──────────┐
│ 📖 总阅读  │ 📅 今日   │ 📝 累计   │ 🔥 连续   │
│   12 篇   │   3 篇   │ 8,500词  │   5天    │
└──────────┴──────────┴──────────┴──────────┘
```

| 指标 | 说明 |
|------|------|
| 总阅读篇数 | 全部历史记录 |
| 今日阅读 | 当天篇数 |
| 累计词数 | 所有文章总词数 |
| 🔥 连续天数 | **真连续**：从最近记录日期往前，断 1 天即重置 |

#### 难度分布柱状图

统计仪表盘下方显示 📊 阅读难度分布柱状图（CET4/CET6/考研/IELTS 四分类）。

#### 记录列表

每条记录：标题、日期、⏱️时长、📝词数、难度星标。支持按来源筛选 + ⭐仅收藏过滤。

### 5.6 阅读状态自动恢复

每次分析完成后自动存入 `reading_state` 表。刷新页面或下次启动时自动恢复文章和分析结果（不重新调用 API）。

### 5.7 数据库自动备份 🛡️

每次启动应用时自动执行：
- 将 `cache.db` 复制到 `backups/cache_YYYYMMDD.db`
- 同一天幂等（不会重复备份）
- 自动清理 7 天前的旧备份
- 备份失败不影响应用运行

---

## 6. 模块说明

### 6.1 config.py

`get_api_key() → str` — 从 `.env` 加载密钥，未配置时抛 `RuntimeError`。

### 6.2 services/ai_service.py

**类**：`AIService`（dataclass）

| 字段 | 默认值 | 说明 |
|------|--------|------|
| `api_key` | (必填) | DeepSeek API Key |
| `model` | `deepseek-chat` | 模型名称 |
| `temperature` | `0.3` | |
| `max_tokens` | `4096` | |
| `timeout` | `60` | 秒 |
| `base_url` | `api.deepseek.com/v1/chat/completions` | |

**方法**：`chat()` / `chat_json()` / `chat_json_fallback()`

### 6.3 services/backup_service.py

`run_backup(db_path, backup_dir, keep_days=7) → bool` — 每日自动备份，幂等，自动清理。

### 6.4 agents/translator.py

`translate_article(text) → dict` — `{"paragraphs": [{...}]}` 或降级 `{"raw_translation": "..."}`

### 6.5 agents/vocabulary.py

`extract_vocabulary(text) → list[dict]` — `[{word, phonetic, meaning, sentence}]`，失败返回 `[]`

### 6.6 agents/grammar.py

`analyze_grammar(text) → list[dict]` — `[{sentence, analysis}]`，失败返回 `[]`

### 6.7 agents/difficulty.py

`analyze_difficulty(text) → dict` — `{level, score, stars, reason}`
- AI 模式优先（`temperature=0.1`）
- 降级规则模式（500+ CET6 词汇库 + 句长统计）

### 6.8 rss_fetcher.py

`fetch_feed(url, name)` / `fetch_all_feeds()` — requests + feedparser 双阶段解析

### 6.9 article_cache.py

`is_duplicate` / `save_article` / `get_all_articles` / `delete_article` / `prune_old_articles` / `get_latest_fetch_date` / **`get_read_links()`**

### 6.10 utils.py

`extract_article_text(url)` — newspaper3k → BS4 双引擎
`clean_text(text)` — 空白清理

### 6.11 database/vocab_repository.py

| 函数 | 说明 |
|------|------|
| `save_word` | 收藏单词 |
| `is_saved` | 是否已收藏 |
| `get_all_words` | 列表（支持搜索） |
| `delete_word` | 删除 |
| `mark_known` | 标记掌握程度 |
| `get_word_count` | 总数 |
| `get_word_growth` | 累积增长数据（折线图用） |
| `get_due_words` | 待复习词（闪卡用） |

### 6.12 database/history_repository.py

| 函数 | 说明 |
|------|------|
| `save_reading_history` | 保存阅读记录，返回 ID |
| `get_reading_history` | 列表（支持来源/收藏筛选） |
| `toggle_favorite` | 切换收藏 |
| `delete_history` | 删除 |
| `get_reading_stats` | 四维统计（含真连续天数） |
| `get_difficulty_distribution` | 难度分布（柱状图用） |
| `save_reading_state` | 保存当前阅读状态 |
| `load_reading_state` | 恢复上次状态 |

---

## 7. 数据库表结构

所有表存储在 `D:\smartread\cache.db`（SQLite），程序启动时自动建表。

### 7.1 articles（文章缓存）
```sql
id INTEGER PK, title TEXT, link TEXT UNIQUE,
source TEXT, published TEXT, fetched_at TIMESTAMP
```

### 7.2 saved_words（生词本）
```sql
id INTEGER PK, word TEXT, phonetic TEXT, meaning TEXT,
example TEXT, source_article TEXT, created_at TIMESTAMP,
review_count INTEGER DEFAULT 0, known_level INTEGER DEFAULT 0
```
`known_level`：0=新词, 1=认识, 2=掌握

### 7.3 reading_history（阅读记录）
```sql
id INTEGER PK, title TEXT, source TEXT, link TEXT,
article_text TEXT, word_count INTEGER,
difficulty TEXT, difficulty_score INTEGER, difficulty_stars TEXT,
read_at TIMESTAMP, reading_minutes INTEGER DEFAULT 0,
favorite INTEGER DEFAULT 0
```

### 7.4 reading_state（状态恢复）
```sql
id INTEGER PK CHECK(id=1), article_text TEXT,
article_source TEXT, analysis_results TEXT, updated_at TIMESTAMP
```
单行表，存储最新一次分析内容。

### 7.5 backups/（自动备份目录）
```
backups/
├── cache_20260515.db
├── cache_20260516.db
└── cache_20260517.db  ← 最新
```

---

## 8. API 参考

### DeepSeek 调用配置

| 参数 | 值 |
|------|-----|
| 端点 | `https://api.deepseek.com/v1/chat/completions` |
| 模型 | `deepseek-chat` |
| temperature | 0.3 / 0.1（难度评级） |
| max_tokens | 4096 / 200（难度评级） |
| timeout | 60s（Agent）/ 30s（RSS）/ 15s（正文提取）|

### 命令行速查

```powershell
cd D:\smartread && streamlit run app.py        # 启动
pip install -r requirements.txt                 # 安装依赖
Remove-Item D:\smartread\cache.db               # 重置数据库
python -c "from config import get_api_key; print(get_api_key()[:12])"  # 查 Key
```

---

## 9. 常见问题

### Q1：点击"开始阅读"后需要等多久？

4 Agent 并发执行，响应时间已优化 50%+。取决于 DeepSeek API 速度。

### Q2：刷新页面后分析结果还在吗？

**在。** 自动存入 `reading_state` 表，刷新恢复。

### Q3：打卡天数怎么算的？

真连续统计：从最近有记录的日期往回数，断 1 天即归零。

### Q4：数据库坏了怎么办？

`backups/` 目录有每日自动备份（保留 7 天）。复制最新的 `cache_YYYYMMDD.db` 覆盖 `cache.db` 即可恢复。

### Q5：怎么用闪卡复习？

生词本页面点击 `🃏 开始复习`，每次 10 张卡片，点「认识了」或「还不会」。已掌握的词不再出现。

### Q6：文章怎么标记已读？

分析过的文章自动标记 `✅ 已读`。勾选「仅显示未读」过滤已读文章。

### Q7：如何切换 AI 模型？

修改 `services/ai_service.py` 中的 `DEFAULT_MODEL` 变量。

---

*SmartRead v2.0 — 让英语阅读从"看一看"变为"学进去"*
